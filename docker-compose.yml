version: '3'


services:
  db:
    image: postgres:15.3
    restart: always
    ports:
      - "5439:5432"
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    container_name: container_tania_db
    volumes:
      - ./postgres:/var/lib/postgresql/data